# Echo Chamber Co-Evolution Framework — Dependencies
# =====================================================
# Compatibile con: Mac/CPU (locale) e Kaggle Runtime (CUDA).
# PyTorch Geometric richiede installazione separata (vedi NOTE sotto).
#
# Installazione standard (locale):
#   pip install -r requirements.txt
#
# NOTE — PyTorch Geometric (PyG):
#   Su Mac CPU:
#     pip install torch-geometric
#   Su Kaggle (CUDA 12.x, torch 2.x):
#     pip install pyg-lib torch-scatter torch-sparse torch-cluster \
#         torch-spline-conv torch-geometric \
#         -f https://data.pyg.org/whl/torch-2.1.0+cu121.html
# =====================================================

# --- Core ---
numpy>=1.26
scipy>=1.12

# --- Graph ---
networkx>=3.3
ogb>=1.3.6               # Open Graph Benchmark (ogbl-collab loader)

# --- Community Detection ---
python-louvain>=0.16      # import community

# --- Machine Learning ---
torch>=2.1               # CPU wheels inclusi su Mac via pip
# torch-geometric  -> vedi NOTE sopra (installazione separata)
scikit-learn>=1.4

# --- LLM ---
google-generativeai>=0.8
openai>=1.30             # anche per Ollama (OpenAI-compatible)

# --- Config & Logging ---
pyyaml>=6.0
tqdm>=4.66
rich>=13.7               # output terminale formattato

# --- Notebook ---
jupyter>=1.0
ipykernel>=6.29

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0

# --- Utility ---
python-dotenv>=1.0       # caricamento .env per API keys
